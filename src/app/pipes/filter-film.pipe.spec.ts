import { FilterFilmPipe } from './filter-film.pipe';

describe('FilterFilmPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFilmPipe();
    expect(pipe).toBeTruthy();
  });
});
